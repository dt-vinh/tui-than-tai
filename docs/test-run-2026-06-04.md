# Test run - 2026-06-04

## Kết quả tổng quan

Trạng thái: PASS cho toàn bộ test tự động khả thi trên máy hiện tại.

Command chính:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run-full-tests.ps1
```

## Các nhóm đã chạy

| Nhóm kiểm thử | Phạm vi | Kết quả |
| --- | --- | --- |
| Backend API | `/health`, register/login, duplicate email, invalid login, categories seed, sync push/pull/update/delete, receipt upload | PASS |
| Android JVM unit test | Parser số tiền, gợi ý category từ OCR/label, title inference | PASS |
| Android debug build | `:app:assembleDebug` | PASS |
| Android release bundle | `:app:bundleRelease` với Kotlin compiler in-process | PASS |
| Emulator smoke | Cài app lên AVD `SafeSign_API35`, onboarding, Home, Tools, screenshot và UI dump | PASS |

## Artifact tạo ra

| Artifact | Đường dẫn |
| --- | --- |
| Debug APK | `android/app/build/outputs/apk/debug/app-debug.apk` |
| Release AAB | `android/app/build/outputs/bundle/release/app-release.aab` |
| UI dump/screenshot | `test-results/onboarding.*`, `test-results/home.*`, `test-results/tools.*` |
| Test summary | `test-results/summary.txt` |

## Lỗi đã debug trong test run

1. `scripts/run-full-tests.ps1` bị lỗi parser do chuỗi tiếng Việt mojibake trong Windows PowerShell.
   - Fix: viết lại script bằng chuỗi ASCII ổn định, tự dò Android SDK, clear app data trước emulator smoke.

2. Biến `$home` trong PowerShell trùng biến hệ thống read-only `HOME`.
   - Fix: đổi thành `$homeUi`.

3. UIAutomator không expose text `Capture` trên floating action button vì FAB chỉ expose vùng clickable, không expose label trong dump.
   - Fix: smoke test chuyển sang kiểm tra các text thật trên Home: `Lucky Wallet`, `Manual entry`, `Reports`, `No expenses yet`.

## Chưa thể kiểm tự động nếu thiếu dữ liệu/môi trường thật

Các mục dưới đây cần bạn cung cấp thêm hoặc chạy trên môi trường thật:

1. Ảnh hóa đơn/đồ vật thật để xác nhận chất lượng OCR + image labeling end-to-end.
   - Cần ít nhất 5 ảnh: hóa đơn rõ, hóa đơn mờ/thiếu sáng, bill tiếng Việt, bill tiếng Anh, ảnh đồ ăn/đồ vật không phải bill.

2. Camera thật trên điện thoại Android.
   - Emulator đã smoke UI, nhưng không xác minh được chất lượng camera vật lý, lấy nét, ánh sáng, và permission flow trên từng hãng máy.

3. Cloudflare Tunnel/domain thật.
   - Backend API đã pass local test. Chưa kiểm được `https://api.<domain>` vì chưa có domain/tunnel credential.

4. Play Store signing/upload.
   - AAB đã build được, nhưng chưa có release keystore/Play Console để xác minh ký bản phát hành và upload Store.

5. GitHub upload.
   - Thư mục hiện chưa phải git repository, máy chưa có `git`/`gh` trong PATH, và chưa có repo URL/token để push.
