# Testing and evidence rules

- Do not claim success without evidence.
- For build tasks, show the exact Gradle command and relevant output.
- For runtime crashes, ask for logcat and cite the stack trace cause.
- Prefer narrow tests first, then full `scripts/run-full-tests.ps1` when stable.
- Track remaining known issues explicitly.
